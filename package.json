{
  "name": "codeex-ai-static-notice",
  "version": "1.0.0",
  "description": "Static notice page for CodeEx AI Flask app",
  "scripts": {
    "build": "cp -r static/* public/ || xcopy static public\\ /E /I || echo 'Files copied'",
    "dev": "echo 'Static files - no dev server needed'"
  },
  "dependencies": {},
  "devDependencies": {}
}